<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ML.App.ViewModels"
             xmlns:app="using:ML.App"
             x:Class="ML.App.Views.TableView"
             x:DataType="vm:TableViewModel">
  <DockPanel Margin="20">
    <!-- the position worm (P6): lower is better, top line = 1st -->
    <Border DockPanel.Dock="Top" Classes="ml-panel" Margin="0,0,0,12"
            IsVisible="{Binding WormVisible}">
      <StackPanel Spacing="6">
        <TextBlock Classes="ml-muted" Text="{Binding WormLabel}"/>
        <Panel Width="560" Height="84" HorizontalAlignment="Left">
          <Rectangle Height="1" Fill="{DynamicResource MlBorder}" VerticalAlignment="Top" Margin="0,4,0,0"/>
          <Rectangle Height="1" Fill="{DynamicResource MlBorder}" VerticalAlignment="Center"/>
          <Rectangle Height="1" Fill="{DynamicResource MlBorder}" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
          <Polyline Points="{Binding WormPoints}" Stroke="{DynamicResource MlPrimary}"
                    StrokeThickness="2.5" StrokeJoin="Round"/>
        </Panel>
      </StackPanel>
    </Border>
    <!-- empty state: a table with no rows is a void, say why -->
    <TextBlock DockPanel.Dock="Top" Classes="ml-muted" Margin="0,0,0,8"
               Text="No standings yet — the table fills in once the season kicks off."
               IsVisible="{Binding !Rows.Count}"/>
    <!-- zone legend, in words (counts come from league data, so lines only name real zones) -->
    <TextBlock DockPanel.Dock="Bottom" Classes="ml-muted" Margin="4,8,0,0"
               Text="{Binding LegendText}" IsVisible="{Binding LegendVisible}"/>
    <DataGrid ItemsSource="{Binding Rows}" IsReadOnly="True"
            GridLinesVisibility="Horizontal" HeadersVisibility="Column">
    <DataGrid.Columns>
      <!-- zone band + position + movement vs last matchday -->
      <DataGridTemplateColumn Header="#" Width="64">
        <DataGridTemplateColumn.CellTemplate>
          <DataTemplate x:DataType="vm:TableEntry">
            <Grid ColumnDefinitions="8,26,Auto">
              <Border Width="3" HorizontalAlignment="Left" VerticalAlignment="Stretch"
                      CornerRadius="1.5" Background="{Binding ZoneBrush}"/>
              <TextBlock Grid.Column="1" Text="{Binding Pos}" VerticalAlignment="Center"
                         Foreground="{Binding PosBrush}" FontWeight="{Binding Weight}"/>
              <TextBlock Grid.Column="2" Text="{Binding Movement}" FontSize="9"
                         VerticalAlignment="Center" Foreground="{Binding MovementBrush}"/>
            </Grid>
          </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
      </DataGridTemplateColumn>
      <DataGridTemplateColumn Header="" Width="40">
        <DataGridTemplateColumn.CellTemplate>
          <DataTemplate x:DataType="vm:TableEntry">
            <Panel Width="26" Height="26" Margin="4,3">
              <Border CornerRadius="13" Background="{Binding BadgeBrush}">
                <TextBlock Text="{Binding Badge}" FontSize="9" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <Image Source="{Binding Logo}" Width="26" Height="26"/>
            </Panel>
          </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
      </DataGridTemplateColumn>
      <!-- your row reads bold; everyone else stays regular -->
      <DataGridTemplateColumn Header="Team" Width="*">
        <DataGridTemplateColumn.CellTemplate>
          <DataTemplate x:DataType="vm:TableEntry">
            <TextBlock Text="{Binding Team}" VerticalAlignment="Center" Margin="12,0,0,0"
                       FontWeight="{Binding Weight}"/>
          </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
      </DataGridTemplateColumn>
      <DataGridTextColumn Header="P" Binding="{Binding P}" Width="46"/>
      <DataGridTextColumn Header="W" Binding="{Binding W}" Width="46"/>
      <DataGridTextColumn Header="D" Binding="{Binding D}" Width="46"/>
      <DataGridTextColumn Header="L" Binding="{Binding L}" Width="46"/>
      <DataGridTextColumn Header="GF" Binding="{Binding GF}" Width="46"/>
      <DataGridTextColumn Header="GA" Binding="{Binding GA}" Width="46"/>
      <DataGridTextColumn Header="GD" Binding="{Binding GD}" Width="46"/>
      <DataGridTextColumn Header="Pts" Binding="{Binding Pts}" Width="56"/>
    </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
