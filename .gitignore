# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo

# IDE
.vs/
.idea/
.vscode/

# Test output
TestResults/
*.trx
*.coverage

# Cargo build output of the vendored stats host
tools/vendor/efootball-re/memprobe/host/target/

# The career save itself. Never commit a database.
*.db
*.db-shm
*.db-wal
*.sqlite

# Game files must never end up in the repo. Only anonymised exports belong in /samples.
*.bin
*.bak
*.cpk
*.pak

# Raw screenshots
*.jpg
*.jpeg

# The editor's bundled player database: 13 MB of third-party data, not ours to
# redistribute. Its shape is recorded in samples/schema-players.md instead.
samples/editor-bundled-players.csv

# Real exports: game data, not ours to redistribute. Schema docs are the record.
bins/
samples/*.csv
bins-evomod/
bins-evomod-full/
EvoMod_6.0/
*.cpk
*.rar
tools/vendor/sider-full/

build/
*.bak

# Avalonia app build screenshot
build/mlapp_screenshot.png

build/mfl_reference/

build/master.db
build/rfs_ability_offsets.json

# Downloaded mod packs (user assets, not source)
*_Toriga*/
*_Toriga*.zip
HDGraphic_Toriga.zip
tools/__pycache__/
build/pc0000_paths.txt
build/iostore_assets.txt
build/stats_map.json
build/ce/cmd.txt
build/ce/cmd.done
build/ce/result.json
tools/__pycache__/
tokens/

# ---- world-asset stores: multi-GB, regenerated from local packs by tools/ ----
facepack/
Uran Faces Megapack/
Uran Faces Megapack.zip
EVO_GP_HEAVY/
EVO_GP_HEAVY.rar
assets/dvx_logos/
assets/dvx_flags/
assets/dvx_comps/
assets/gen_badges/
assets/badges/
dist/
careers/
# raw exports at repo root (FM/Genie data, not ours to redistribute)
/*.csv
/*.html
# stray downloads
ChatGPT Installer.exe
Microsoft.Services.Store.winmd
*.winmd
# downloaded data packs & exports living at repo root (delete nothing, commit nothing)
sortitoutsi*/
sortitoutsi*.zip
FM26PlayerExport*/
soccer-bunny*.zip
scratchpad/
scratchpad_pack_uids.txt
design/
*.zip

# ---- third-party tools, mod packs and game-data dumps: supplied locally, never committed ----
# CRI File System Tools (proprietary, CRI Middleware) — place at repo root; tools/ml_apply.py expects
# CRI_File_System_Tools_v2.40.13.0/crifilesystem v2.40.13.0/cpkmakec.exe
CRI_File_System_Tools*/
# eFootball-Player-Editor.exe, eFootball-WESYS-Unzlib-Tool.exe and any other tool dropped at the root
/*.exe
# extracted game trees (bins/ is above; these are its siblings)
bins-*/
TomMod/
New folder/
# decoded game-table dumps and one-off research scripts
scratch_*
*.dec
cpkmaker.out.csv
# python bytecode
__pycache__/
*.pyc
# rejected asset batches
assets/dvx_comps_bad*/
# the owner's own player photos (SquadView "Set photo…" copies them here) and stray face downloads
custom_faces/
/face_*.webp
